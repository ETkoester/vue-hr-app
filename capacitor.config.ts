import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.xxxxx.xxxxxxxxxx",
    appName: "xxxxxxxxxxxx",
    webDir: "dist",
    server: {
        androidScheme: "https",
    },
};

export default config;
